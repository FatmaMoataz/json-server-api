{
  "name": "json-server-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
